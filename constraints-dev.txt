# The exact versions the test suite is green against (§4.2).
#
# `pyproject.toml` publishes *ranges*, because pinning a published
# distribution to `numpy==2.5.1` produces an unsolvable resolution beside
# anything else that wants numpy, and the user cannot fix it. This file is
# the other half of that trade: the ranges say what should work, this says
# what was actually verified.
#
# Reproduce the environment the suite was run in:
#
#     pip install -e ".[dev]" -c constraints-dev.txt
#
# A constraints file rather than a requirements file on purpose: it pins
# versions without *adding* dependencies, so it stays correct if the extras
# in pyproject.toml change.
#
# Refresh with:
#     pip list --format=freeze > constraints-dev.txt   # then re-add this header
#
# Recorded 2026-08-06 on CPython 3.14, Windows.
#
# Not listed: `build`, which you need to make a wheel locally
# (`pip install build && python -m build`, after `python tools/build_ui.py`).
# It builds the distribution rather than being part of it, and the release
# workflow installs it itself.

# --- runtime -----------------------------------------------------------
lief==1.0.0
capstone==5.0.9
numpy==2.5.1
fastapi==0.141.1
uvicorn==0.52.1
pillow==12.3.0

# --- test ---------------------------------------------------------------
pytest==9.1.1
httpx==0.28.1
iced-x86==1.21.0
ziglang==0.16.0

# --- transitive, pinned so a rebuild is reproducible ---------------------
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
h11==0.16.0
idna==3.18
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
pydantic==2.13.4
starlette==1.3.1
